spring:
  application:
    name: Financial_Record

  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://ep-fancy-poetry-ansgqzzg-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DATASOURCE_USER:neondb_owner}
    password: ${DATASOURCE_PASSWORD:npg_QlxzcZYd4Dj6}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: 8082

jwt:
  secret: ${JWT_SECRET:f4e2b8c9d0a1b2c3d4e5f6a7b8c9d0a1b2c3d4e5f6a7b8c9d0a1b2c3d4e5f6a7}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
